# dbt_project.yml — DORA ICT Incident Intelligence Pipeline
# Status: PLACEHOLDER — to be filled in Phase 5

name: dora_incident_pipeline
version: "1.0.0"
config-version: 2

profile: dora_incident_pipeline

model-paths: ["models"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dora_incident_pipeline:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
