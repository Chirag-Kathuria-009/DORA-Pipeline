# dbt package dependencies — install with `dbt deps`.
# dbt_utils provides generate_surrogate_key() used by the staging models.
packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
